# Core simulation
pvlib>=0.11.0
pandas>=2.0.0
numpy>=1.24.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9

# Data validation
pydantic>=2.0.0

# PDF generation
reportlab>=4.0.0

# AI agents (optional — falls back gracefully if Ollama is not running)
crewai>=0.80.0
crewai-tools>=0.14.0
langchain-ollama>=0.2.0

# HTTP client (used by pvlib / crewai internals)
httpx>=0.27.0
